
# Put a list of properties that can be passed into a configuration
# Have to be key value pairs and will end up as environment variables
# prefixed with SPANR_CFG_VARNAME
MyProperty: 'MyValue'
MyOtherProperty: 'MyOtherValue'
